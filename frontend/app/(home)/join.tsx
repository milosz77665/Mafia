import CustomText from '@/components/CustomText';
import { FC } from 'react';

interface JoinProps {}

const Join: FC<JoinProps> = () => {
  return <CustomText style={{ fontSize: 50 }}>Join</CustomText>;
};

export default Join;
